[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-audio-tests"
version = "0.2.0"
description = "Voice interaction configs, TTS/STT pipeline settings, and audio samples"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["audio-tests","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-audio-tests"
Repository = "https://github.com/mxn2020/minions-audio-tests"

[tool.hatch.build.targets.wheel]
packages = ["minions_audio_tests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
